<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="40dp"
    android:layout_margin="8dp">

    <android.support.v7.widget.AppCompatImageView
        android:id="@+id/image_back_search"
        android:layout_width="48dp"
        android:layout_height="40dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:clickable="true"
        android:focusable="true"
        android:paddingBottom="8dp"
        android:paddingEnd="12dp"
        android:paddingStart="12dp"
        android:paddingTop="8dp"
        app:srcCompat="@drawable/ic_arrow_cancel_search" />

    <ru.taxcom.mobile.android.searchlibrary.util.textview.CustomEditText
        android:id="@+id/edit_text_search"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:background="@android:color/transparent"
        android:ellipsize="end"
        android:gravity="center_vertical"
        android:imeOptions="actionDone"
        android:inputType="textCapWords"
        android:paddingEnd="8dp"
        android:paddingStart="12dp"
        android:textColorHint="@color/textSearchHint"
        android:textSize="17sp" />

    <android.support.v7.widget.AppCompatImageView
        android:id="@+id/image_clear_search"
        android:layout_width="48dp"
        android:layout_height="40dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:clickable="true"
        android:focusable="true"
        android:paddingBottom="8dp"
        android:paddingEnd="12dp"
        android:paddingStart="12dp"
        android:paddingTop="8dp"
        android:visibility="gone"
        app:srcCompat="@drawable/ic_clear_search" />
</LinearLayout>